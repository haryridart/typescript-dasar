describe('Data Type', function(){
    it('should declare data', function(){
        const name: string ="Hary Ridart";
        const balance: number = 100000000;
        const isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});