"use strict";
describe('Data Type', function () {
    it('should declare data', function () {
        const name = "Hary Ridart";
        const balance = 100000000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
